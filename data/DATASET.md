# BarMate dataset

Generated by `python -m sim.build`. Deterministic: same seed, same output.

## What is real and what is not

This distinction matters more than any other fact about this dataset, and it
should be stated plainly in the write-up rather than buried.

**Simulated.** The venue's own books: stock levels, POS sales, physical counts,
purchase orders, reservations, the rota, closing reports and shift-group chat.
No real bar publishes these, and no public source exists for them. They are
generated by `sim/engine.py` from an explicit demand model, and every figure
follows from that model rather than being sampled at random.

**Real.** Every external signal that drives a forecast.

| Source | File | Provenance |
|---|---|---|
| Israeli sports broadcasts | `public/broadcasts.csv` | Sport5 and livegames.co.il, `source_url` preserved per row |
| Weather | `external/weather.csv` | Open-Meteo ERA5 archive, keyless |
| Jewish calendar | `external/holidays.csv` | Hebcal REST API, keyless, CC-BY-4.0 |

Weather and holidays are fetched by `scripts/fetch_external.py`, which must be
run from a machine with general internet access. **If they are absent the build
still succeeds and the corresponding multipliers stay switched off.** Nothing is
estimated or substituted in their place. A forecast that cites weather when no
weather was loaded would be a fabrication, so the code makes that impossible
rather than merely discouraged.

**Carried over from the course data.** Product names, categories, unit costs and
prices; the phrasing patterns and ambiguity styles in the closing reports; the
Hebrew slang and message shapes in the shift-group chat.

## The anchor

Everything is frozen at **Sunday 2026-06-14, 18:00**, an hour before doors.

That date is not arbitrary. It is the only point in the calendar where real
broadcast data covers both tonight and the days ahead: ten listings that Sunday,
eight of them live, including two fixtures involving Israeli clubs, then Maccabi
Tel Aviv against Bayern Munich in the EuroLeague on the Monday.

Broadcast coverage stops on Wednesday the 17th. This is deliberate and left
unpatched. Asked about the coming weekend, the honest answer is that fixtures
past Wednesday are unconfirmed, and saying so is the behaviour GT003 and GT004
are testing for.

**The agent must not see anything after the anchor.** The tool layer is
responsible for enforcing this. `public/whatsapp_messages.csv` is already
truncated at 18:00 on the 14th, but `sales`, `reservations` and `staff_schedule`
deliberately run through 2026-06-20, because that held-out tail is what makes
forecast accuracy measurable. Filter on load, not by hoping.

## Layout

```
data/
  public/           everything the agent may read
    products.csv            61 SKUs: 55 bottled, 5 draught kegs, 1 coffee
    suppliers.csv           3, with delivery days and order minimums
    cocktails.csv           7, transcribed from RAG-006
    cocktail_recipes.csv    27 ingredient lines, millilitres and unit fractions
    staff.csv               7, with role and authority level
    staff_schedule.csv      who worked which night
    sales.csv               POS lines, servings and revenue
    inventory_counts.csv    physical counts, Wednesdays and Sundays, as written
    orders.csv              purchase orders with invoiced quantity
    reservations.csv        bookings with party size and status
    shift_reports.csv       closing reports, free text, Hebrew and English
    whatsapp_messages.csv   shift-group chat, Hebrew
    broadcasts.csv          REAL fixture listings
    knowledge/              14 operations documents, one file per RAG doc
  ground_truth/     held out; never load into Supabase or Pinecone
    true_inventory.csv        actual closing stock, per product per night
    count_errors.csv          what each count got wrong, and by how much
    incidents.csv             the planted events, with cause and magnitude
    incident_events.csv       per-night stock effect of each incident
    shift_report_truth.csv    parse key for all 293 reports
    anchor_discrepancies.csv  book versus physical at the anchor, all 61 SKUs
    scenarios.csv             9 evaluation scenarios with answer keys
    daily_demand.csv          the demand index actually used each night
  external/         fetched, not generated
  MANIFEST.json     provenance and row counts for the build
```

## Two truths

The engine tracks physical stock and what humans wrote down as separate
quantities. The agent only ever sees the second, plus the POS. The gap between
them is the problem BarMate exists to solve, so it is modelled rather than
approximated.

Book stock is what the agent must compute:

```
book = last reported count
     + units invoiced as delivered since that count
     - units the POS accounts for since that count
```

Note *invoiced*, not received. When a delivery arrives short, the books believe
the invoice. That belief is the discrepancy.

## Signal against noise

Count error has three modes rather than being uniform noise, because counting
sealed bottles is near exact and only the open bottle on the rack gets
eyeballed: 90% counted right, 8% a misjudged open bottle (0.05 to 0.30 units),
2% a bottle missed entirely (1.0 unit).

This is not only a realism point. A flat noise floor of plus or minus two units
would bury a dropped bottle and a walkout below the detection threshold, making
those incidents unfindable by any agent however good. Measured at the anchor:

- median gap across all 61 products: **0.01 units**
- largest gap among unaffected control products: **0.43 units** (Jose Cuervo)
- smallest planted signal: **0.51 units** (Johnnie Walker Black)

Those last two were 0.29 and 0.52 before the weather multipliers were switched
on. Higher throughput lifts the noise floor, and the separation is now 0.08
units rather than 0.23. Detection still works, but a materiality threshold
hardcoded near 0.5 would now sit inside the noise. Derive it from the count
distribution rather than picking a constant.

That is what `app/tools/inventory.py` does. Every closed window between two
counts is replayed and the residual recorded, and the 90th percentile of a
product's own residuals becomes its materiality threshold. The spread that
justifies it is wide: 0.27 units on Tanqueray against 14.70 on Coca-Cola, so
no single constant could serve both. Against the anchor discrepancies the
envelope flags Coca-Cola, Bombay Sapphire, Carlsberg 30L, Havana Club, Grey
Goose and Jameson with no false positives, and correctly declines to flag the
four control kegs, Jose Cuervo and the happy-hour lines.

The happy-hour lines fall out of this for free. Doubled depletion against
single-rung revenue is a persistent shortfall, so those lines carry a wide
envelope by measurement rather than by exception: House White tolerates 4.20
units where a shelf gin tolerates 0.27. What the envelope does not do is
excuse a gap that clears it. Carlsberg 30L is both a happy-hour line and the
product two kegs were pulled from, and a rule that stopped at "happy hour
explains it" would bury the keg that GT004 turns on.

## Planted incidents

22 incidents, 10 in the fortnight before the anchor and 12 spread across the
preceding nine months. The background set exists so the final fortnight is not
conspicuously the only interesting stretch, which would let an agent score well
by always looking in the same place.

Incidents are **inputs to the simulation, not annotations on it**. When one says
a bottle was dropped, the engine destroys the stock, so the ledger, the counts
and the reconciliation arithmetic all agree without anything being hardcoded.

Headline cases at the anchor:

| Incident | Product | Book | Physical | Gap | Mentioned by anyone? |
|---|---|---|---|---|---|
| INC-041 | Bombay Sapphire | 6.95 | 0.00 | 6.95 | No |
| INC-050 | Carlsberg 30L | 6.08 | 1.15 kegs | 4.93 | Chat only |
| INC-048 | Coca-Cola | 245.50 | 221.50 | 24.00 | Chat only |
| INC-044 | Havana Club | 6.49 | 5.49 | 1.00 | No |
| INC-043 | Grey Goose | 6.91 | 5.91 | 1.00 | Chat only |

The logged-versus-unlogged column is the point. Distinguishing a loss someone
mentioned from one nobody did is RAG-013's reconciliation matrix, and it cannot
be done from the POS alone.

## The discriminator worth knowing about

Four products show gaps of 1.49 to 1.76 units at the anchor that are **not**
shrinkage: House Red, House White, Bacardi and Gordon's. These are happy-hour
lines, where RAG-005 specifies that physical depletion runs at double the rung-up
volume between 18:00 and 20:30.

An agent that skips the knowledge base will report them as theft. One that
retrieves RAG-005 will explain them. This emerged from modelling the protocol
honestly rather than being designed in, and it separates retrieval-grounded
reasoning from pattern matching better than any scenario written on purpose.
Scenario GT006 tests it directly.

## Evaluation scenarios

Nine, in `ground_truth/scenarios.csv`, each with a machine-checkable
`answer_key` and a `passes_if` condition. GT001 through GT005 are the original
course scenarios with figures corrected to what the data actually contains.
GT006 through GT009 cover knowledge-explained variance, silent loss detection,
invoice discrepancy, and refusing to invent a product that is not in the
catalogue.

## Known limits

- **Weather and holiday multipliers are live** as of the 2026-08-19 build.
  `fetch_external.py` pulled 293 days of ERA5 weather and 50 holiday days, and
  every figure in this document reflects that. Re-running the fetch and build
  will move the numbers again, so do it before final evaluation, not after.
- **Event multipliers apply only to the four dates with real broadcast data.**
  The rest of the calendar runs at 1.0. This means the 1.5x football uplift is a
  policy rule from RAG-004, not a coefficient estimated from history, and the
  agent should say so rather than implying it was learned.
- **Cocktail garnishes are not tracked.** Citrus, mint and ice appear in the
  knowledge base but only mint has a SKU.
- **Stations are modelled as an attribute, not as separate stock pools.** A
  product marked `outside` is not counted separately from the inside bar.
- 27 of 61 products appear in closing reports. Slow movers such as Diplomatico
  are rarely mentioned, which is realistic but means report-parsing accuracy is
  measured on a subset.
